run 3 iterations of hyperparameter search

the epoch is changed from 2000 to 10000 to guarantee convergence in iter2_final

all models are test on teset set 'states10k.csv'

SE reward function leads to lower ss error, but at the cost of slower convergence